training:
  batch_size: 32
  num_epochs: 6
  learning_rate: 1e-4
  weight_decay: 0.01
  seed: 42

model:
  name: image_only   # options: text_only, image_only, multimodal
  image_model_id: google/vit-base-patch16-224
  max_length: 256
