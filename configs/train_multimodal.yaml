training:
  batch_size: 32
  num_epochs: 10
  learning_rate: 1e-4
  weight_decay: 0.01
  seed: 42

model:
  name: multimodal   # options: text_only, image_only, multimodal
  text_model_id: vinai/bertweet-base
  image_model_id: google/vit-base-patch16-224
  max_length: 256
