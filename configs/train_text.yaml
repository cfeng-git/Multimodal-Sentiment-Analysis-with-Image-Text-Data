training:
  batch_size: 32
  num_epochs: 20
  learning_rate: 1e-4
  weight_decay: 0.01
  seed: 42

model:
  name: text_only   # options: text_only, image_only, multimodal
  text_model_id: vinai/bertweet-base
  max_length: 256
